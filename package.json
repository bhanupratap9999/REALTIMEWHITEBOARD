{
  "name": "realtime-whiteboard-ot",
  "version": "1.0.0",
  "description": "Real-Time Multi-Party Whiteboard with Operational Transformation (OT) conflict resolution",
  "scripts": {
    "postinstall": "npm --prefix server install && npm --prefix client install --include=dev",
    "install:all": "npm install && npm --prefix server install && npm --prefix client install --include=dev",
    "build": "npm --prefix server install && npm --prefix client install --include=dev && npm --prefix client run build",
    "build:client": "npm --prefix client install --include=dev && npm --prefix client run build",
    "build:server": "npm --prefix server install",
    "start": "node server/src/server.js",
    "dev": "concurrently --kill-others-on-fail --names \"SERVER,CLIENT\" --prefix-colors \"cyan,magenta\" \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "test": "npm --prefix server test"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "vite": "^5.2.0"
  },
  "keywords": [
    "whiteboard",
    "operational-transformation",
    "ot",
    "real-time",
    "collaboration",
    "socket.io",
    "fabricjs",
    "react",
    "mongodb"
  ],
  "author": "",
  "license": "MIT"
}
