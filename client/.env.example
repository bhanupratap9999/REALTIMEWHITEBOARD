# Client Environment Variables
# Leave blank or omit in single-service deployments (Express serves frontend directly)
# Set to your backend API URL if deploying frontend separately (e.g. on Vercel or Netlify)
VITE_SERVER_URL=https://your-backend-api.onrender.com
